# Third-party notices

`app/vendor/cloudbase.full.js` is the CloudBase browser SDK bundle used by this
template. It remains subject to the upstream Tencent CloudBase SDK licence and
terms. Before redistributing a modified SDK bundle, obtain the current licence
text from its upstream project.

This repository contains no Tencent Cloud credentials, environment data,
photos, invitations, sessions, or user records.
